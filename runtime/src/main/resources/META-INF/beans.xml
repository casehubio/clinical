<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="https://jakarta.ee/xml/ns/jakartaee"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/beans_4_0.xsd"
       bean-discovery-mode="all">
  <alternatives>
    <!--
      casehub-ledger's JpaLedgerEntryRepository is @Alternative.
      Clinical does not use casehub-qhorus (which provides the default
      MessageLedgerEntryRepository in the AML stack). Activate the JPA
      implementation directly here.
    -->
    <class>io.casehub.ledger.runtime.repository.jpa.JpaLedgerEntryRepository</class>
  </alternatives>
</beans>
